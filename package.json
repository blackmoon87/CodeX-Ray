{
  "name": "codex-ray",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.6.2",
  "scripts": {
    "prepare": "pnpm --filter @codex-ray/core build",
    "build": "pnpm -r build",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.1.0"
  }
}
